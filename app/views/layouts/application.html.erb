<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  </head>

  <body>
    <header class="w-100 p-3" style="background-color: #f5f5f5;">
      <nav class="navbar d-flex justify-content-between">
        <div>
          <%= link_to 'All posts', posts_path, class: 'btn btn-secondary me-2' %>
        </div>
        <div>
          <% if user_signed_in? %>
            <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: 'btn btn-danger me-2', style: 'color: white;' %>
          <% else %>
            <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary me-2 text-white' %>
            <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary text-white' %>
          <% end %>
        </div>
      </nav>
    </header>
    <main class="p-5">
      <%= yield %>
    </main>

    <% if notice %>
      <div class="notice p5"><%= notice %></div>
    <% end %>
    <% if alert %>
      <div class="alert p5"><%= alert %></div>
    <% end %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
